# gcp-vm-infra-opentofu-example


Para inicializar o diretório e baixar os plugins necessários:

```bash
tofu init

```
Planejando a criação da infra:

```bash
tofu plan
```

Criando a Infraestrutura:

```bash
tofu apply -auto-approve
```